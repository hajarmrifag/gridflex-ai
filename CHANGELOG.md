# Changelog

## 0.2.0

- Add a React workspace with dispatch playback, scenario comparison, forecasting, stress testing and optimization.
- Add device-local snapshots, configuration links and JSON/CSV exports.
- Add a validated FastAPI service with bounded requests, compute concurrency and caches.
- Replace repeated dataframe writes with array operations in daily demand shifting.
- Separate peak minimization from throughput minimization in the optimizer; expose charging and terminal-state constraints.
- Validate timestamps and numeric inputs, disclose interpolation in the German sample, and exclude imputed observations from forecast evaluation.
- Add production Docker packaging, Render configuration, browser journeys and engine/API checks.
- Keep the Streamlit research interface available alongside the new workspace.
